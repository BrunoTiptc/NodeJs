let idade = 30; // Inteiro (integer)
let valor = 100.50; // Decimal (float)

console.log(10 % 2); // Resto da divisão (modulo)
console.log(10 ** 2); // Potenciação (exponenciação)    -- ao quadrado

let erro = "texto" * 2;
console.log(erro); // NaN (Not a Number) - resultado de uma operação inválida com números

let infinito = 10 / 0;
console.log(infinito); // Infinity - resultado de uma divisão por zero

console.log(Math.round(4.7)); // Arredonda para o inteiro mais próximo (5)
console.log(Math.floor(4.7)); // Arredonda para baixo (4)
console.log(Math.ceil(4.2)); // Arredonda para cima (5) 

console.log(Math.random()); // Gera um número aleatório entre 0 (inclusive) e 1 (exclusivo)
console.log(Math.random() * 100); // Gera um número aleatório entre 0 e 100
console.log(Math.floor(Math.random() * 100)); // Gera um número inteiro aleatório entre 0 e 99  
console.log(Math.PI); // Valor de pi (3.141592653589793)

